# تحديث شاشة اللعب - الوضع الراسي

## المشكلة
في الوضع الراسي (Portrait)، كانت أزرار الأسئلة قد تخرج من حدود الحاوية (Container) مما يسبب مشاكل في الواجهة.

## الحل المطبق

### 1. تحسين حساب أحجام المكونات
تم تعديل الحسابات في دالة `CategoryCard` للوضع الراسي:

**قبل:**
- استخدام حساب معقد ومتغير لعدد الصفوف والأزرار
- قد يؤدي لأحجام غير متناسبة

**بعد:**
- حساب ثابت: **صفين (2 rows)** بـ **3 أزرار في كل صف**
- توزيع منتظم وآمن للمساحة

### 2. تقليل الحشو والمسافات
- تقليل `cardPadding` من **8** إلى **6**
- تقليل `horizontalGaps` من **3** إلى **2**
- تقليل `verticalGaps` من **3** إلى **2**
- هذا يوفر مساحة أكثر للأزرار

### 3. حساب ذكي للعرض والارتفاع
```javascript
// عرض الأزرار
const maxButtonWidth = (buttonsSpace - totalHorizontalGaps) / buttonsPerRow;

// ارتفاع الأزرار
const maxButtonHeight = (contentHeight - totalVerticalGaps) / numRows;

// اختيار الحجم الأصغر لضمان الملاءمة
const buttonSize = Math.min(maxButtonWidth, maxButtonHeight, 45);
```

### 4. ضمان عدم تجاوز الحاوية
- عرض البطاقة: `Math.min(availableWidth - 8, contentWidth + (cardPadding * 2))`
- ارتفاع البطاقة: `Math.min(buttonSize * numRows + gaps + padding, availableHeight - 4)`

### 5. تحسين توزيع الأزرار
الأزرار الآن مقسمة إلى صفين:
- **الصف الأول**: سهل + متوسط (أول أزرار)
- **الصف الثاني**: متوسط (ثاني أزرار) + صعب

## النتائج

✅ جميع أزرار الأسئلة **تبقى داخل الحاوية** في الوضع الراسي
✅ توزيع **متناسق وآمن**
✅ **بدون تقطع أو تجاوز** للحدود
✅ تجربة مستخدم **محسّنة**

## الملفات المعدلة
- `screens/GameScreen.js` - دالة `CategoryCard`
