# تحديث ميزة الألعاب المحفوظة

## الميزات الجديدة المضافة:

### 1️⃣ خيار "استكمال اللعبة"
- **المتطلب**: للألعاب التي لم تكتمل بعد
- **الفائدة**: يمكن استرجاع اللعبة من حيث توقفت
- **النقاط**: تُحتسب من جديد (نقاط جديدة كاملة)
- **الزر**: يظهر فقط إذا كانت اللعبة `isCompleted = false`

```
استكمال اللعبة:
- استرجاع جميع البيانات السابقة (الفرق، الفئات، الأسئلة)
- استرجاع النقاط السابقة للفرق
- الاستمرار من حيث توقفت
- تحديث نفس السجل في قاعدة البيانات
```

### 2️⃣ خيار "إعادة تشغيل اللعبة"
- **الفائدة**: لعب نفس اللعبة من البداية
- **النقاط**: نقاط جديدة (تبدأ من 0)
- **الحفظ**: لا يتم حفظها كلعبة جديدة
- **العداد**: لا يزيد عدد الألعاب

```
إعادة التشغيل:
- نفس الفرق والفئات والأسئلة
- نقاط جديدة (من 0)
- لا يتم حفظ كلعبة جديدة في البيانات
- لا يؤثر على عدد الألعاب
```

## الملفات المعدلة:

### 1. `services/savedGamesService.js`
**إضافات:**
- ✅ دالة `continueGame()` - لاستكمال اللعبة
- ✅ تعديل `saveGame()` - عدم حفظ الألعاب المعاد تشغيلها
- ✅ تحسين `getSavedGame()` - استخدام `getDoc` بدلاً من `query`

**الكود الجديد:**
```javascript
// عدم حفظ الألعاب المعاد تشغيلها
if (gameData.isReplaying) {
  console.log('هذه لعبة معاد تشغيلها - لن يتم حفظها كلعبة جديدة');
  return null;
}

// استكمال اللعبة من حيث توقفت
continueGame: async (savedGame) => {
  return {
    ...
    scores: { ...savedGame.scores }, // استرجاع النقاط السابقة
    isContinuing: true
  };
}
```

### 2. `components/SavedGamesModal.js`
**إضافات:**
- ✅ زر "استكمال" - يظهر فقط للألعاب غير المكتملة
- ✅ زر "إعادة تشغيل" - يظهر دائماً
- ✅ دالة `handlePlayGame()` - تدعم كلا الموديين

**الأزرار الجديدة:**
```javascript
// استكمال (يظهر فقط إذا !isCompleted)
<TouchableOpacity onPress={() => handlePlayGame(item, 'continue')}>
  <MaterialIcons name="redo" size={18} />
  استكمال
</TouchableOpacity>

// إعادة تشغيل (يظهر دائماً)
<TouchableOpacity onPress={() => handlePlayGame(item, 'replay')}>
  <MaterialIcons name="replay" size={20} />
  إعادة تشغيل
</TouchableOpacity>
```

### 3. `screens/RoundResults.js`
**تعديلات:**
- ✅ عدم حفظ الألعاب المعاد تشغيلها
- ✅ تحديث الألعاب المستكملة بدلاً من إنشاء جديدة
- ✅ التحقق من `isReplaying` و `isContinuing`

**الكود:**
```javascript
// فقط حفظ الألعاب الجديدة
if (!gameData.isReplaying && !gameData.isContinuing) {
  await SavedGamesService.saveGame(...);
}

// تحديث الألعاب المستكملة
if (gameData.isContinuing) {
  await SavedGamesService.updateSavedGame(gameData.savedGameId, {
    scores: scores,
    isCompleted: true
  });
}
```

## التدفقات:

### تدفق الاستكمال:
```
لعبة غير مكتملة
    ↓
اضغط "استكمال"
    ↓
استرجاع النقاط السابقة
    ↓
الاستمرار من حيث توقفت
    ↓
النهاية: تحديث نفس السجل
    ↓
لا يتم إنشاء سجل جديد
```

### تدفق إعادة التشغيل:
```
أي لعبة (مكتملة أو لا)
    ↓
اضغط "إعادة تشغيل"
    ↓
نقاط جديدة (من 0)
    ↓
نفس الفرق والفئات والأسئلة
    ↓
النهاية: لا يتم الحفظ
    ↓
لا يزيد عدد الألعاب
```

## قاعدة البيانات:

### حقول جديدة:
```javascript
{
  // ...حقول موجودة
  replayCount: 0,        // عدد مرات إعادة التشغيل
  isCompleted: boolean,  // هل اكتملت اللعبة
  savedGameId: string    // معرف اللعبة الأصلية (للألعاب المستكملة)
}
```

## الفوائد:

✅ **للاعبين:**
- استكمال الألعاب غير المكتملة
- تدريب على نفس الأسئلة بدون إهدار كريديت
- لا يؤثر على عداد الألعاب

✅ **للإحصائيات:**
- الألعاب المعاد تشغيلها لا تُحتسب
- الألعاب المستكملة تُحدّث السجل الأصلي
- بيانات دقيقة وموثوقة
