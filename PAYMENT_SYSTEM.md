# 💳 نظام الدفع والأرصدة - Payment System

## 📌 نظرة عامة
يستخدم تطبيق "فكّر" نظام Freemium متطور يجمع بين اللعب المجاني (رصيد محدود) وإمكانية شراء باقات إضافية. حالياً، نستخدم **نظام دفع وهمي (Mock Payment)** لأغراض الاختبار والتطوير، وهو مصمم ليكون مطابقاً تماماً لنظام الدفع الحقيقي المستقبلي (TAP Payment).

---

## 🚀 نظام الدفع الوهمي (Mock Payment)

لقد بنينا **منصة دفع وهمية احترافية** تسمح لك باختبار كامل نظام الشراء والشحن الأوتوماتيكي **بدون الحاجة لأي بوابة دفع حقيقية**.

### ✅ الميزات الرئيسية
*   **دفع أوتوماتيكي فوري:** الرصيد يُضاف مباشرة إلى Firestore خلال 1-3 ثواني.
*   **محاكاة واقعية:** واجهة احترافية، تأخير واقعي للمعالجة، ومحاكاة Webhook.
*   **آمنة تماماً:** بيانات وهمية فقط، معزولة في مجموعة `mockTransactions`.
*   **سهلة الاستخدام:** بيانات البطاقة مملوءة مسبقاً للتجربة السريعة.

### 🔄 دورة العمل
1.  المستخدم ينقر "شراء" ويختار الباقة.
2.  تظهر نافذة الدفع الوهمي (Mock Payment Modal).
3.  المستخدم يؤكد الدفع (بيانات وهمية).
4.  النظام يعالج الطلب ويحاكي Webhook.
5.  يتم تحديث رصيد المستخدم في Firestore فوراً.
6.  تظهر رسالة النجاح.

### 📁 الملفات الأساسية
*   `services/MockPaymentService.js`: معالجة منطق الدفع والمحاكاة.
*   `components/MockPaymentModal.jsx`: واجهة المستخدم لعملية الدفع.
*   `components/CreditsDisplay.js`: عرض الرصيد وزر الشراء.

---

## 💰 نظام الأرصدة (Credits)

### كيف يعمل؟
*   كل مستخدم جديد يحصل على **2 لعبة مجانية**.
*   كل لعبة تستهلك **1 رصيد**.
*   يمكن للمستخدم شراء باقات إضافية (5، 15، 30، 100 لعبة).
*   الرصيد يُحفظ في Firestore تحت حقل `credits`.

### الباقات المتوفرة
| الباقة | عدد الألعاب | السعر (وهمي) | الوصف |
|--------|-------------|--------------|-------|
| Starter | 5 ألعاب | $4.99 | للمبتدئين |
| Pro | 15 لعبة | $12.99 | الأكثر شعبية |
| Expert | 30 لعبة | $24.99 | للمحترفين |
| Ultimate | 100 لعبة | $79.99 | توفير كبير |

---

## 🧪 الاختبار (Testing)

يمكنك اختبار النظام باستخدام البيانات الوهمية التالية:
*   **رقم البطاقة:** `4242 4242 4242 4242`
*   **الاسم:** أي اسم (مثلاً `Test User`)
*   **التاريخ:** أي تاريخ مستقبل (مثلاً `12/25`)
*   **CVC:** أي 3 أرقام (مثلاً `123`)

للمزيد من سيناريوهات الاختبار، راجع ملفات الاختبار في الكود المصدري.

---

## 🔒 الأمان والحماية

*   **Firestore Rules:** قواعد صارمة تمنع تعديل الرصيد يدوياً من قبل المستخدم.
*   **Server-Side Logic:** إضافة الرصيد تتم عبر Cloud Functions (أو محاكاتها الآمنة) وليس من العميل مباشرة في النظام الحقيقي.
*   **Logs:** تسجيل جميع عمليات الشراء في مجموعة `creditLogs` للمراجعة.

---

## 🔜 الانتقال إلى الدفع الحقيقي (TAP Payment)

النظام مصمم ليكون جاهزاً للانتقال إلى TAP Payment بسهولة:
1.  استبدال `MockPaymentService` بـ `TAPPaymentService`.
2.  تحديث `MockPaymentModal` ليعرض واجهة TAP.
3.  تغيير متغيرات البيئة لتفعيل الدفع الحقيقي.

---

**ملاحظة:** هذا النظام مخصص حالياً للاختبار والتطوير. لا تقم بإدخال بيانات بطاقات ائتمان حقيقية.

---

## 🛠️ التفاصيل التقنية (Technical Implementation)

### هيكلية قاعدة البيانات (Database Schema)

#### 1. حقل `credits` في مستند المستخدم (`users/{userId}`)
```javascript
{
  uid: "user123",
  credits: {
    remaining: 2,              // الرصيد المتبقي
    initialFree: 2,            // عدد الألعاب المجانية الأولية
    totalPurchased: 0,         // إجمالي الألعاب المشتراة
    totalUsed: 0,              // إجمالي الألعاب المستخدمة
    totalGranted: 0,           // إجمالي المكافآت المجانية
    lastUsedAt: timestamp,     // آخر استخدام للرصيد
    lastPurchaseAt: timestamp  // آخر عملية شراء
  }
}
```

#### 2. مجموعة `mockTransactions` - سجل المعاملات الوهمية
```javascript
{
  userId: "user123",
  transactionId: "mock_1735000000000_abc12345",
  status: "completed",
  amount: 12.99,
  credits: 15,
  currency: "USD",
  paymentMethod: "mock_card",
  type: "mock",
  timestamp: serverTimestamp()
}
```

### منطق استهلاك الرصيد
يتم استخدام `CreditsService.consumeCreditForGame()` عند بدء اللعبة:
1.  يقرأ الرصيد الحالي.
2.  يتحقق إذا كان `remaining >= 1`.
3.  يخصم 1 من الرصيد ويحدث الإحصائيات في معاملة ذرية (Transaction).

### قواعد الحماية (Firestore Security Rules)
تم تطبيق قواعد صارمة في `firestore.rules` لضمان:
*   منع تعديل الرصيد يدوياً.
*   السماح بخصم 1 فقط عند اللعب.
*   السماح بإضافة الرصيد فقط عبر عمليات شراء موثقة أو منح نظامي.

