# 💳 نظام الدفع والأرصدة - Payment System

## 📌 نظرة عامة
يستخدم تطبيق "فكّر" نظام Freemium متطور يجمع بين اللعب المجاني (رصيد محدود) وإمكانية شراء باقات إضافية. حالياً، نستخدم **نظام دفع Upayment** للدفع الإلكتروني الحقيقي.

---

## 🚀 نظام الدفع (Upayment)

لقد قمنا بدمج **بوابة الدفع Upayment** لتوفير تجربة دفع آمنة وموثوقة.

### ✅ الميزات الرئيسية
*   **دفع آمن:** يتم الدفع عبر بوابة Upayment المعتمدة.
*   **دعم KNET وBayad:** دعم كامل لبطاقات KNET والبطاقات الائتمانية.
*   **شحن فوري:** الرصيد يُضاف مباشرة إلى حساب المستخدم بعد نجاح العملية.
*   **دعم منصات متعددة:**
    - **الويب**: نافذة دفع منفصلة (Popup Window) لتجاوز قيود Content Security Policy
    - **الهاتف**: WebView مدمج داخل التطبيق

### 🔄 دورة العمل

#### على الويب (Web):
1.  المستخدم يختار باقة ويضغط "الدفع الآن"
2.  يتم إنشاء رابط دفع عبر API الخاص بـ Upayment
3.  تفتح نافذة دفع منفصلة (Popup Window)
4.  المستخدم يكمل عملية الدفع في النافذة الجديدة
5.  بعد الانتهاء، النافذة تُغلق تلقائياً
6.  يطلب النظام من المستخدم تأكيد نجاح الدفع
7.  يتم تحديث الرصيد في Firestore

#### على الهاتف (Mobile):
1.  المستخدم يختار باقة ويضغط "الدفع الآن"
2.  يتم إنشاء رابط دفع عبر API الخاص بـ Upayment
3.  تظهر نافذة الدفع (WebView) داخل التطبيق
4.  المستخدم يكمل عملية الدفع
5.  عند النجاح، يتم اكتشاف رابط النجاح تلقائياً
6.  يتم تحديث الرصيد في Firestore فوراً

### 📁 الملفات الأساسية
*   `services/UpaymentService.js`: خدمة التعامل مع API الخاص بـ Upayment
    - `createPaymentLink()`: إنشاء رابط دفع جديد
    - `handlePaymentSuccess()`: معالجة نجاح الدفع وإضافة الرصيد
*   `components/UpaymentModal.jsx`: واجهة المستخدم لعملية الدفع
*   `components/CreditsDisplay.js`: عرض الرصيد وزر الشراء
*   `app/purchase.js`: صفحة الشراء الرئيسية

---

## 💰 نظام الأرصدة (Credits)

### كيف يعمل؟
*   كل مستخدم جديد يحصل على **2 لعبة مجانية**.
*   كل لعبة تستهلك **1 رصيد**.
*   يمكن للمستخدم شراء باقات إضافية.
*   الرصيد يُحفظ في Firestore تحت حقل `credits`.

### الباقات المتوفرة (أسعار بالدينار الكويتي KWD)
| الباقة | عدد الألعاب | السعر | الوصف |
|--------|-------------|-------|-------|
| Single | 1 لعبة | 0.100 د.ك | لعبة واحدة |
| Pair | 2 لعبة | 0.150 د.ك | عرض جيد |
| Bundle | 5 ألعاب | 0.250 د.ك | توفير أكثر |
| Maximum | 20 لعبة | 1.000 د.ك | أفضل عرض |

### سياسة الأسعار
- **العملة**: الدينار الكويتي (KWD)
- **سعر اللعبة الواحدة**:
  - Single: 0.100 د.ك (100 فلس)
  - Pair: 0.075 د.ك (75 فلس) - توفير
  - Bundle: 0.050 د.ك (50 فلس) - توفير أكبر
  - Maximum: 0.050 د.ك (50 فلس) - أفضل قيمة

---

## 🔒 الأمان والحماية

*   **Firestore Rules:** قواعد صارمة تمنع تعديل الرصيد يدوياً من قبل المستخدم.
*   **Server-Side Logic:** إضافة الرصيد تتم عبر التحقق من المعاملة.
*   **Logs:** تسجيل جميع عمليات الشراء في مجموعة `transactions` للمراجعة.

---

## 🛠️ التفاصيل التقنية (Technical Implementation)

### هيكلية قاعدة البيانات (Database Schema)

#### 1. حقل `credits` في مستند المستخدم (`users/{userId}`)
```javascript
{
  uid: "user123",
  credits: {
    remaining: 2,              // الرصيد المتبقي
    initialFree: 2,            // عدد الألعاب المجانية الأولية
    totalPurchased: 0,         // إجمالي الألعاب المشتراة
    totalUsed: 0,              // إجمالي الألعاب المستخدمة
    totalGranted: 0,           // إجمالي المكافآت المجانية
    lastUsedAt: timestamp,     // آخر استخدام للرصيد
    lastPurchaseAt: timestamp  // آخر عملية شراء
  }
}
```

#### 2. مجموعة `transactions` - سجل المعاملات الحقيقية
```javascript
{
  userId: "user123",
  transactionId: "ord_1735000000000_abc12345",
  status: "completed",
  amount: 0.100,
  credits: 1,
  currency: "KWD",
  paymentMethod: "upayment",
  type: "real",
  createdAt: "2025-12-06T15:57:24.879Z",
  timestamp: serverTimestamp()
}
```

### منطق استهلاك الرصيد
يتم استخدام `CreditsService.consumeCreditForGame()` عند بدء اللعبة:
1.  يقرأ الرصيد الحالي.
2.  يتحقق إذا كان `remaining >= 1`.
3.  يخصم 1 من الرصيد ويحدث الإحصائيات في معاملة ذرية (Transaction).

### قواعد الحماية (Firestore Security Rules)
تم تطبيق قواعد صارمة في `firestore.rules` لضمان:
*   منع تعديل الرصيد يدوياً.
*   السماح بخصم 1 فقط عند اللعب.
*   السماح بإضافة الرصيد فقط عبر عمليات شراء موثقة أو منح نظامي.

