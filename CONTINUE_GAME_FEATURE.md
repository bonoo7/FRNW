# تحديث ميزة استكمال الألعاب - النسخة الأخيرة

## ✅ المشكلة المحلولة:

### الميكانيكية:
- كل جولة لها عدد محدد من الأسئلة حسب عدد الفرق
- مثال: 2-3 فرق = 36 سؤال
- إذا تم استخدام **جميع الأسئلة** = اللعبة **مكتملة** ✓
- إذا تم الضغط على **إنهاء اللعب** قبل انتهاء الأسئلة = اللعبة **غير مكتملة** ⏸

## 🎮 الميزات المضافة:

### 1. تحديد حالة اللعبة تلقائياً
```javascript
// يتم حساب isCompleted بناءً على:
// إذا: answeredQuestions >= totalQuestions → اللعبة مكتملة
// وإلا: → اللعبة غير مكتملة
```

### 2. عرض حالة اللعبة في "ألعابي"
- ✅ **شارة خضراء** مع رمز ✓ للألعاب المكتملة
- ⏸ **شارة برتقالية** مع رمز ⏳ للألعاب غير المكتملة

### 3. أزرار العمل
**للألعاب غير المكتملة (⏸ غير مكتملة):**
- 🔄 زر **"استكمال"** - لاستكمال من حيث توقفت
- 🎮 زر **"إعادة تشغيل"** - للعب من البداية
- 🗑️ زر **"حذف"** - لحذف اللعبة

**للألعاب المكتملة (✓ مكتملة):**
- 🎮 زر **"إعادة تشغيل"** - للعب من البداية
- 🗑️ زر **"حذف"** - لحذف اللعبة

## 📝 الملفات المعدلة:

### 1. `screens/RoundResults.js`
**إضافات:**
```javascript
// دالة لتحديد حالة اللعبة
const isGameCompleted = () => {
  const { totalQuestions, answeredQuestions } = gameData.statistics;
  return totalQuestions > 0 && answeredQuestions >= totalQuestions;
};

// استخدام الحالة عند الحفظ
const completed = isGameCompleted();
await SavedGamesService.saveGame(currentUser.uid, {
  ...gameData,
  isCompleted: completed,
  scores: scores
});
```

### 2. `components/SavedGamesModal.js`
**إضافات:**
```javascript
// شارة تظهر حالة اللعبة
{!item.isCompleted && (
  <View style={styles.badge}>
    <MaterialIcons name="hourglass-empty" size={14} />
    <Text>غير مكتملة</Text>
  </View>
)}
```

## 🔄 التدفق الكامل:

### سيناريو 1: لعبة غير مكتملة (توقف مبكر)
```
اللعب → إنهاء اللعب المبكر
    ↓
حفظ مع isCompleted: false
    ↓
ظهور شارة ⏸ "غير مكتملة"
    ↓
خيارات:
  ① استكمال → استرجاع النقاط السابقة + الاستمرار
  ② إعادة تشغيل → بدء جديدة بنقاط 0
  ③ حذف → إزالة اللعبة
```

### سيناريو 2: لعبة مكتملة (استخدام جميع الأسئلة)
```
اللعب → استخدام جميع 36 سؤال
    ↓
حفظ مع isCompleted: true
    ↓
ظهور شارة ✓ خضراء "مكتملة"
    ↓
خيارات:
  ① إعادة تشغيل → بدء جديدة بنقاط 0
  ② حذف → إزالة اللعبة
```

### سيناريو 3: استكمال لعبة غير مكتملة
```
استكمال من حيث توقفت
    ↓
استرجاع الأسئلة المتبقية
    ↓
استرجاع النقاط السابقة للفرق
    ↓
الاستمرار في اللعب
    ↓
إذا استخدمت كل الأسئلة: تحديث isCompleted: true
    ↓
نفس السجل يُحدّث (لا سجل جديد)
```

## 📊 قاعدة البيانات:

### حقل isCompleted:
```javascript
{
  id: string,
  userId: string,
  roundName: string,
  teams: string[],
  categories: string[],
  selectedQuestions: object[],
  scores: { [teamName]: number },
  statistics: {
    totalQuestions: number,      // عدد الأسئلة الكلي
    answeredQuestions: number,   // الأسئلة المستخدمة
    ...
  },
  isCompleted: boolean,  // ✓ جديد: true إذا تم استخدام جميع الأسئلة
  createdAt: ISO,
  updatedAt: ISO
}
```

## 🎯 الفوائد:

✅ **تجربة لاعب محسّنة:**
- يمكن التوقف والاستكمال لاحقاً
- لا تخسر التقدم الذي أحرزته
- واضح أي لعبة مكتملة وأي لا

✅ **بيانات دقيقة:**
- تمييز واضح بين الألعاب المكتملة والمتوقفة
- إحصائيات موثوقة

✅ **مرونة اللعب:**
- إعادة التشغيل بدون حفظ كلعبة جديدة
- استكمال دون إعادة من البداية
