# ميزة إعادة تشغيل الألعاب المحفوظة

## الميزات الجديدة

### 1. حفظ تلقائي للألعاب
- عند انتهاء أي جولة، يتم حفظ جميع بيانات اللعبة تلقائياً في Firebase:
  - **عدد الفرق**
  - **أسماء الفرق**
  - **الفئات المختارة**
  - **الأسئلة المستخدمة**
  - **نتائج الجولة** (درجات الفرق)

### 2. زر "ألعابي" في الصفحة الرئيسية
- يظهر زر جديد مع شارة تعرض عدد الألعاب المحفوظة
- الزر يفتح مودال يعرض جميع الألعاب المحفوظة

### 3. عرض الألعاب المحفوظة
المودال يعرض لكل لعبة:
- **اسم الجولة**
- **التاريخ والوقت**
- **عدد الفرق والفئات والأسئلة**
- **حالة اللعبة** (مكتملة أو قيد التشغيل)

### 4. إعادة تشغيل اللعبة
- يمكن الضغط على أي لعبة لفتح تفاصيلها
- زر "إعادة تشغيل" لبدء اللعبة بنفس البيانات
- **لا يتم خصم كريديت** عند إعادة التشغيل
- تبدأ النقاط من 0 مع نفس الفرق والفئات والأسئلة

### 5. إدارة الألعاب المحفوظة
- يمكن حذف أي لعبة من القائمة
- تأكيد حذف قبل إزالة اللعبة

## الملفات المضافة/المعدلة

### ملفات جديدة:
1. **`services/savedGamesService.js`**
   - خدمة كاملة لإدارة الألعاب المحفوظة في Firebase
   - حفظ واستعادة وحذف الألعاب
   - تحويل اللعبة المحفوظة إلى لعبة جديدة

2. **`components/SavedGamesModal.js`**
   - مكون المودال لعرض الألعاب المحفوظة
   - واجهة رسومية سهلة وجميلة
   - عرض تفاصيل كل لعبة

### ملفات معدلة:
1. **`screens/RoundResults.js`**
   - إضافة استدعاء حفظ اللعبة بعد نهاية الجولة
   - استدعاء `SavedGamesService.saveGame()` مع بيانات كاملة

2. **`screens/HomeScreen.js`**
   - إضافة حالة `showSavedGames` و`savedGamesCount`
   - إضافة زر "ألعابي" مع شارة الرقم
   - تحميل عدد الألعاب المحفوظة ديناميكياً
   - إضافة `SavedGamesModal` في نهاية الـ render

## التدفق الكامل

### عند انتهاء جولة:
```
نهاية الجولة → RoundResults
    ↓
    → saveGameStatistics() (حفظ الإحصائيات)
    ↓
    → SavedGamesService.saveGame() (حفظ بيانات اللعبة في Firebase)
    ↓
    → العودة للصفحة الرئيسية
```

### عند الضغط على "ألعابي":
```
الضغط على زر "ألعابي"
    ↓
    → فتح SavedGamesModal
    ↓
    → تحميل جميع الألعاب من Firebase
    ↓
    → عرض قائمة الألعاب المحفوظة
```

### عند اختيار لعبة:
```
اختيار لعبة → عرض التفاصيل
    ↓
    → الضغط على "إعادة تشغيل"
    ↓
    → SavedGamesService.replayGame() تحويل اللعبة
    ↓
    → بدء لعبة جديدة بنفس البيانات (بدون خصم كريديت)
```

## قاعدة البيانات

### مجموعة `savedGames`:
```javascript
{
  id: string,
  userId: string,
  roundName: string,
  teams: string[],
  categories: string[],
  selectedQuestions: object[],
  scores: { [teamName]: number },
  statistics: object,
  createdAt: ISO timestamp,
  updatedAt: ISO timestamp,
  isCompleted: boolean
}
```

## التحسينات المستقبلية

- [ ] ترتيب الألعاب حسب التاريخ/الاسم/عدد الفرق
- [ ] بحث وتصفية الألعاب
- [ ] عرض احصائيات قارنة بين إعادة التشغيل والأصلية
- [ ] مشاركة الألعاب مع لاعبين آخرين
- [ ] نسخ احتياطية آلية
